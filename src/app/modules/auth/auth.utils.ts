import jwt from 'jsonwebtoken';

// Create a token with jwt payload and secret key with expiration time
export const createToken = (
  jwtPayload: { userId: string; role: string },
  secret: string,
  expiresIn: string,
) => {
  return jwt.sign(jwtPayload, secret, {
    expiresIn,
  });
};
